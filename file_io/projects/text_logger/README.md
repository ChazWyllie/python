# Text Logger

## Description
A simple text logger for logging messages with timestamps and log levels (`INFO`, `WARNING`, `ERROR`). Supports optional log file rotation when the file exceeds a certain size.

## Features
- Log messages with timestamps.
- Categorize logs using levels (`INFO`, `WARNING`, `ERROR`).
- Automatically rotate logs when they exceed a specified size.
